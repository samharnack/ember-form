{{#each @items as |item index|}}
  {{yield
    (fn @remove item)
    (hash
      property=(component "form/property"
        get=(fn @get item)
        id=(call @ref index)
        ref=(fn @ref index)
        set=(fn @set item)
      )
    )
    index
  }}
{{/each}}