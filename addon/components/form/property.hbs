<div ...attributes>
  {{#let (hash
    ref=(fn @ref @name)
    set=(fn this.setEvent @set @name)
    get=(fn @get @name)
  ) as |property|}}
    {{yield (hash
      id=(call property.ref)
      label=(component
        "form/property/label"
        for=(call property.ref)
        name=@name
      )
      input=(component
        "form/property/input"
        id=(call property.ref)
        name=@name
        set=property.set
        get=property.get
        type=(or @type "text")
      )
      description=(component
        "form/property/description"
        for=(call property.ref)
      )
      name=@name
      set=property.set
      get=property.get
    )}}
  {{/let}}
</div>